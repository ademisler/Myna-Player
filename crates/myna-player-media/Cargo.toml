[package]
name = "myna-player-media"
version = "0.1.0"
edition = "2024"
description = "FFmpeg and FFprobe integration for Myna Player"
license = "GPL-3.0-or-later"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
myna-player-core = { path = "../myna-player-core", version = "0.1.0" }

[dev-dependencies]
tempfile = "3"
