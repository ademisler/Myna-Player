[package]
name = "myna-player-core"
version = "0.1.0"
edition = "2024"
description = "Shared domain models and look-ahead scheduling for Myna Player"
license = "GPL-3.0-or-later"

[dependencies]
serde = { version = "1", features = ["derive"] }
