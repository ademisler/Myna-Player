[package]
name = "myna-player-jobs"
version = "0.1.0"
edition = "2024"
description = "Deterministic persistent-work scheduling primitives for Myna Player"
license = "GPL-3.0-or-later"

[dependencies]
serde = { version = "1", features = ["derive"] }
myna-player-core = { path = "../myna-player-core", version = "0.1.0" }
